projection;
strict ( 2 );
use draft;

define behavior for ZEAL_C_RAP100_TRAV alias Travel
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}